node_modules/
main.js
*.js.map
webapp/
.vscode/
data.json
src/preview/viewer.min.txt

# Scratch lint configs used for local repro of review findings (see CLAUDE.md);
# meant to be created and deleted within a session, never committed.
eslint.check.config.mjs
review-lint.mjs

# Vite/Vitest auto-generated timestamped config shims.
vitest.config.ts.timestamp-*.mjs

# Local capture workspace for README screenshots (raw shots, helper scripts);
# only the finished images under docs/assets/ are committed.
demo-vault/
